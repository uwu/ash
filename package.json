{
	"name": "@uwu/ash",
	"version": "1.0.1",
	"description": "A TS framework that looks vagely like Elm from a distance.",
	"main": "dist/index.js",
	"module": "dist/index.js",
	"types": "dist/index.d.ts",
	"scripts": {
		"format": "rome format --write .",
		"devbuild": "esbuild src/index.ts --outfile=dist/bundle.js --bundle --format=iife --global-name=ash",
		"prepare": "tsc && npm run devbuild"
	},
	"author": "Yellowsink",
	"license": "Unlicense",
	"dependencies": {
		"htm": "^3.1.1"
	},
	"devDependencies": {
		"esbuild": "^0.18.17",
		"rome": "^12.1.3"
	}
}
